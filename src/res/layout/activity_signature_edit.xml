<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/page_bg"
    tools:context=".NicknameEditActivity" >
	<RelativeLayout 
	    android:id="@+id/nick_titlebar"
	    android:layout_width="match_parent"
	    android:layout_height="50dp"
	    android:background="@drawable/bg_title">
	    <ImageButton 
	        android:id="@+id/signature_ib_back"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:layout_marginLeft="10dp"
            android:background="@drawable/icon_back"
            android:layout_centerVertical="true"/>
	    <TextView 
	        android:id="@+id/nick_tv_title"
	        android:layout_width="wrap_content"
	        android:layout_height="wrap_content"
	        android:layout_centerInParent="true"
	        android:text="个性签名"
	        android:textColor="@color/black"
            android:textSize="20sp"/>
	    <Button
	        android:id="@+id/signature_submit"
	        android:layout_width="50dp"
	        android:layout_height="25dp"
	        android:layout_alignParentRight="true"
	        android:layout_centerVertical="true"
	        android:layout_marginRight="5dp"
	        android:background="@drawable/bg_btn_mini"
	        android:text="确认"
	        android:textSize="15sp"
	        android:textColor="#ffffff"/>
	</RelativeLayout>
	<RelativeLayout 
	    android:id="@+id/nick_body"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_marginTop="20dp"
	    >
		<EditText 
		    android:id="@+id/nick_et_signature"
		    android:layout_width="match_parent"
		    android:layout_height="wrap_content"
		    android:hint="请输入个性签名"
		    android:textSize="24sp"
		    android:paddingTop="2dp"
		    android:paddingBottom="2dp"
		    android:paddingLeft="4dp"
		    android:background="#ffffff"/>
	</RelativeLayout>
</LinearLayout>
